apiVersion: v1
kind: Service
metadata:
    labels:
        app: subscription-service
    name: subscription-service
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app: subscription-service
    
